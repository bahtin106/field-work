# 🔄 МИГРАЦИОННАЯ СТРАТЕГИЯ

## Для существующих компонентов, использующих старый код

### Вариант 1: Немедленная миграция (Рекомендуется)

#### Шаг 1: Замена в login.jsx

```javascript
// ❌ Было
import { supabase } from '../../lib/supabase';
const [email, setEmail] = useState('');
const handleLogin = useCallback(async () => { ... }, []);

// ✅ Стало
import { useAuthLogin } from '../../hooks/useAuthLogin';
const { email, setEmail, handleLogin } = useAuthLogin();
```

#### Шаг 2: Обновление других компонентов

```javascript
// signup.jsx
import { useAuthLogin } from '../../hooks/useAuthLogin';
// Используй то же самое валидацию и обработку ошибок

// password-recovery.jsx
import { mapSupabaseAuthError } from '../../lib/supabaseAuthErrors';
// Используй ту же обработку ошибок
```

#### Шаг 3: Удаление старого кода

```bash
# После завершения миграции удалить:
- Старые валидаторы из компонентов
- Старую обработку ошибок
- Старые хаки (isMountedRef)
```

---

### Вариант 2: Постепенная миграция (Если кода много)

#### День 1: Создать новый код (СДЕЛАНО)

- ✅ authValidation.js
- ✅ supabaseAuthErrors.js
- ✅ useAuthLogin.js
- ✅ Документация

#### День 2: Мигрировать login.jsx (СДЕЛАНО)

- ✅ Использовать useAuthLogin
- ✅ Использовать useTranslation
- ✅ Тестировать вручную

#### День 3: Мигрировать signup (если есть)

- [ ] Создать useAuthSignup (аналогично useAuthLogin)
- [ ] Использовать authValidation и supabaseAuthErrors
- [ ] Тестировать

#### День 4: Мигрировать recovery (если есть)

- [ ] Использовать authValidation
- [ ] Использовать supabaseAuthErrors
- [ ] Тестировать

#### День 5: Очистка и оптимизация

- [ ] Удалить дублирование старого кода
- [ ] Добавить unit-тесты
- [ ] Code review

---

## Обратная совместимость

### Старый код всё ещё работает

```javascript
// ❌ Старый способ (не рекомендуется, но работает)
import { t } from '../../src/i18n';
const message = t('key', 'fallback');

// ✅ Новый способ (рекомендуется)
import { useTranslation } from '../../src/i18n/useTranslation';
const { t } = useTranslation();
```

### Переходный период

Вы можете использовать оба способа одновременно:

```javascript
function Component() {
  // Новый код
  const { t } = useTranslation();

  // Старый код (будет удален позже)
  const oldValue = useOldHook();

  return <Text>{t('key')}</Text>;
}
```

---

## Миграция других компонентов

### Если у вас есть другие формы логина

#### Вариант: OAuth (Google, Apple)

```javascript
// useAuthOAuth.js
export function useAuthOAuth() {
  const handleGoogleLogin = useCallback(async () => {
    const { data, error } = await supabase.auth.signInWithOAuth({
      provider: 'google',
    });

    if (error) {
      const errorKey = mapSupabaseAuthError(error);
      // ... обработка ошибки
    }
  }, []);

  return { handleGoogleLogin };
}
```

#### Вариант: Биометрия

```javascript
// useAuthBiometric.js
export function useAuthBiometric() {
  const handleBiometricLogin = useCallback(async () => {
    const { fingerprint } = await BiometricLib.authenticate();

    // Отправить на сервер
    const { error } = await supabase.functions.invoke('auth-biometric', {
      body: { fingerprint },
    });

    if (error) {
      const errorKey = mapSupabaseAuthError(error);
      // ... обработка ошибки
    }
  }, []);

  return { handleBiometricLogin };
}
```

#### Вариант: SMS OTP

```javascript
// useAuthSMS.js
export function useAuthSMS() {
  const handleSendOTP = useCallback(async (phone) => {
    if (!isValidPhoneNumber(phone)) {
      return { error: 'Invalid phone' };
    }

    const { error } = await supabase.auth.signInWithOtp({
      phone: phone,
    });

    if (error) {
      const errorKey = mapSupabaseAuthError(error);
      // ... обработка ошибки
    }
  }, []);

  return { handleSendOTP };
}
```

---

## Проверка совместимости

### Перед миграцией

```bash
# 1. Убедиться, что нет breaking changes
npm test

# 2. Проверить, что новый код работает
npm run dev

# 3. Тестировать вручную:
# - Вход с правильными данными
# - Вход с неправильными данными
# - Сетевая ошибка
# - Timeout
```

### После миграции

```bash
# 1. Запустить все тесты
npm test

# 2. Проверить в браузере/телефоне
npm run dev

# 3. Проверить performance
npm run build --analyze

# 4. Проверить bundle size
npm run size
```

---

## Откат в случае проблем

### Если новый код сломал что-то

```bash
# 1. Вернуть старый login.jsx из git
git checkout HEAD -- app/\(auth\)/login.jsx

# 2. Сохранить новые utils (они полезны)
# Новые файлы в lib/ и hooks/ можно оставить

# 3. Проверить, что работает
npm run dev

# 4. Исправить проблемы в новом коде
# И попробовать снова
```

---

## План на будущее

### Краткосрочно (1-2 недели)

- [x] Рефакторинг login.jsx
- [ ] Мигрировать другие компоненты (если есть)
- [ ] Добавить unit-тесты для utils
- [ ] Code review

### Среднесрочно (1 месяц)

- [ ] Добавить социальную авторизацию (если нужна)
- [ ] Добавить биометрию (если нужна)
- [ ] Добавить SMS OTP (если нужна)
- [ ] Покрыть интеграционными тестами

### Долгосрочно (3+ месяца)

- [ ] Рефакторинг других форм в приложении (используя те же паттерны)
- [ ] Создание shared UI компонентов для форм
- [ ] Централизованное управление ошибками приложения
- [ ] Advanced фичи (remember me, 2FA и т.д.)

---

## Обучение команды

### Для junior разработчиков

1. Прочитать QUICK_SUMMARY.md (5 мин)
2. Просмотреть ARCHITECTURE.md (15 мин)
3. Запустить примеры из TESTING_EXAMPLES.md (30 мин)

### Для middle разработчиков

1. Прочитать REFACTORING_GUIDE.md (15 мин)
2. Пересмотреть исходный код (30 мин)
3. Написать свой unit-тест (30 мин)

### Для senior разработчиков

1. Провести code review (20 мин)
2. Обсудить архитектурные решения (30 мин)
3. Спланировать расширения (30 мин)

---

## Метрики внедрения

### Что отслеживать

- ✅ Количество успешных логинов
- ✅ Количество ошибок авторизации (по типам)
- ✅ Среднее время загрузки экрана входа
- ✅ Покрытие кода тестами
- ✅ Количество багов после миграции

### Целевые метрики

- ✅ 0 утечек памяти (memory leaks)
- ✅ 99.9% успешных логинов
- ✅ < 100ms на валидацию
- ✅ > 80% покрытие кода
- ✅ 0 регрессионных багов

---

## Контрольный список для запуска

- [ ] Все файлы созданы и без ошибок
- [ ] Tests написаны и проходят
- [ ] Code review пройден
- [ ] Ручное тестирование завершено
- [ ] Документация обновлена
- [ ] Команда обучена новому коду
- [ ] Production ready

---

**Готово к запуску!** 🚀
