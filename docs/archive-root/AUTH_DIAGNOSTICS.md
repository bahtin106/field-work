# Диагностика проблем авторизации

## Проблема: Спинер виснет на кнопке "Вход"

### Причина

`useAuthLogin` успешно выполнял `signInWithPassword`, но:

1. Навигация не происходила автоматически (ожидалась через `onAuthStateChange`)
2. `loading` сбрасывался мгновенно, что создавало мерцание
3. Компонент мог размонтироваться до сброса состояния

### Решение

- Добавлена проверка наличия `session.access_token` после успешного логина
- `loading` сохраняется 1 секунду для плавного перехода
- Логирование процесса для диагностики

---

## Проблема: После перезагрузки приложение не понимает кто залогинен

### Причина

При холодном старте:

1. `supabase.auth.getSession()` возвращал сессию
2. Но `supabase.auth.getUser()` возвращал `null` (таймаут / сетевая задержка)
3. Логика считала пользователя НЕ залогиненным
4. Роль не загружалась в кэш React Query

### Решение

1. **Увеличен таймаут** для `getUser()` с 3s → 4s для первой попытки
2. **Ретрай с задержкой** 1s вместо 800ms
3. **Fallback на session.user**: если `getUser()` не работает, используем `session.user`
4. **Явное кэширование роли** в `initializeApp` через `queryClient.setQueryData(['userRole'], userRole)`
5. **Детальное логирование** на каждом этапе

---

## Ключевые логи для диагностики

Проверьте в консоли следующие метки:

### При логине:

```
🔐 Auth event: SIGNED_IN
👤 Current user: <user-id>
✅ Profile loaded: role=<role>
🎭 User role resolved: <role>
📦 Cached userRole: <role>
📦 Cached profile for user: <user-id>
✅ State updated: isLoggedIn=true, role=<role>
🔄 App remounted with new key
⏰ Session epoch bumped
```

### При холодном старте:

```
getUser attempt 1 failed: <error> (если есть проблема)
Retrying getUser after 1s delay...
Using user from session object as fallback (если getUser не работает)
```

### При загрузке роли:

```
Failed to cache userRole: <error> (если проблема с React Query)
```

---

## Что делать если проблемы остались

### 1. Спинер всё ещё висит

- Проверьте логи: появляется ли `SIGNED_IN` event?
- Проверьте: есть ли `session.access_token` после логина?
- Добавьте точку останова в `useAuthLogin.performLogin` после `signInWithPassword`

### 2. После перезагрузки пользователь не залогинен

- Проверьте: вызывается ли `supabase.auth.getSession()` при старте?
- Проверьте: появляется ли `session.user` в логах?
- Проверьте: есть ли в консоли `Using user from session object as fallback`?
- Убедитесь что AsyncStorage не очищается между запусками

### 3. Роль не определяется

- Проверьте базу данных: есть ли запись в таблице `profiles` для пользователя?
- Проверьте: появляется ли `Profile loaded: role=<role>`?
- Проверьте: есть ли в консоли `Cached userRole`?
- Добавьте точку останова в `getUserRole()`

---

## Архитектурные улучшения (для будущего)

1. **Auth State Machine**: вынести состояния (`restoring`, `authenticated`, `anonymous`) в отдельный провайдер
2. **Stack.Protected**: использовать условную регистрацию экранов вместо ручных `replace`
3. **Optimistic UI**: показывать skeleton с fallback данными до загрузки реальных
4. **Telemetry**: логировать время от старта до готовности интерфейса
5. **Error Boundary**: глобальная граница для перехвата критических ошибок auth

---

## Текущий flow авторизации

### Логин:

1. Пользователь вводит email/password → `handleLogin()`
2. `useAuthLogin.performLogin()` вызывает `supabase.auth.signInWithPassword()`
3. Supabase генерирует событие `SIGNED_IN`
4. `_layout.js` слушает через `onAuthStateChange`
5. Загружаются: профиль, роль, локаль
6. Очищается старый кэш
7. Роль и профиль кладутся в React Query
8. `setIsLoggedIn(true)` → навигация на `/orders`
9. `bumpSessionEpoch()` → экран сбрасывает bootstrap
10. Показывается рабочий интерфейс

### Холодный старт:

1. `initializeApp()` вызывает `getSession()`
2. Если есть `session.access_token` → вызов `getUser()` (с ретраями)
3. Если `validatedUser` получен → загружается роль
4. Роль кэшируется в React Query
5. `setIsLoggedIn(true)` → навигация на `/orders`
6. Показывается рабочий интерфейс
