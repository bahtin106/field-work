# Отчет по исправлению проблемы со сменой пароля

## Проблема
Пользователь меняет пароль, но он не заносится в БД.

## Найденные причины

### 1. Отсутствие логирования изменений пароля
- Нет таблицы в БД для логирования изменений пароля
- Нет информации о том, когда и кем был изменен пароль
- Невозможно отследить проблему через логи

### 2. Недостаточное логирование в edge-функции
- `supabase/functions/update_user/index.ts` не выводит логи об успешности операции
- Нет информации о том, прошла ли операция обновления password в auth.users успешно

### 3. Игнорирование ошибок на фронтенде
- Фронтенд в `app/users/[id]/edit.jsx` игнорирует ошибки сети при обновлении пароля
- Это скрывает реальные проблемы от разработчика

## Решение

### ✅ Сделано:

#### 1. Обновлена edge-функция `supabase/functions/update_user/index.ts`
- Добавлено подробное логирование (`console.log`) для отслеживания процесса обновления пароля
- Функция теперь логирует:
  - Попытку обновления auth credentials
  - Успех или ошибку при обновлении пароля в auth.users
  - Логирование изменения пароля в таблицу password_change_log
- Добавлена обработка `changed_by` для отслеживания, кто изменил пароль

#### 2. Создана таблица password_change_log
Файл: `supabase/migrations/20260211_password_change_log.sql`
- Таблица для логирования всех изменений пароля
- Содержит информацию:
  - user_id: чей пароль был изменен
  - changed_by: кто это сделал (админ или сам пользователь)
  - changed_at: время изменения
  - ip_address, user_agent: дополнительная информация для аудита
- Включен RLS для защиты данных
- Созданы индексы для быстрого поиска

#### 3. Обновлен фронтенд `app/users/[id]/edit.jsx`
- Добавлено подробное логирование при отправке запроса на изменение пароля
- Логируются:
  - Попытка отправки пароля на edge-функцию
  - Ответ от сервера
  - Результат обновления (успех/ошибка и текст ошибки)
- Улучшена обработка ошибок:
  - Реальные ошибки теперь выбрасываются (не скрываются)
  - Сетевые ошибки обрабатываются специально
- Добавлена отправка `changed_by` (ID пользователя, который меняет пароль)

#### 4. Создан SQL скрипт для быстрого развертывания
Файл: `SETUP_PASSWORD_LOGS.sql`
- Содержит все `CREATE TABLE`, `CREATE POLICY`, `CREATE INDEX` для password_change_log
- Можно выполнить в Supabase Studio SQL Editor

### ⏳ Требуется сделать:

#### 1. Применить миграцию в БД на Supabase
**Вариант 1: Через Supabase Dashboard**
- Открыть https://supabase.monitorapp.ru
- Перейти в SQL Editor
- Скопировать содержимое `SETUP_PASSWORD_LOGS.sql`
- Выполнить скрипт
- Убедиться, что таблица создана успешно

**Вариант 2: Через CLI на VPS**
```bash
cd /path/to/field-work
supabase db push  # если настроено
```

#### 2. Развернуть обновленную edge-функцию
**На VPS:**
```bash
cd /path/to/field-work
supabase functions deploy update_user --project-ref xyzabc  # подставьте ваш project-ref
```

Или в Supabase Dashboard:
- Перейти в Edge Functions
- Найти `update_user`
- Заменить содержимое на новый код из `supabase/functions/update_user/index.ts`
- Deploy

#### 3. Протестировать

**Вариант A: В приложении**
1. Откройте приложение field-work
2. Перейдите в редактирование профиля сотрудника
3. Измените пароль
4. Смотрите в браузер консоль (Developer Tools → Console) логи с префиксом `[proceedSave]`
5. Проверьте, что логирование показывает успешное обновление

**Вариант B: Прямой тест в БД**
```sql
-- 1. Проверите таблицу password_change_log
SELECT * FROM public.password_change_log ORDER BY changed_at DESC LIMIT 10;

-- 2. Должны увидеть записи об изменении пароля с timestamp
```

## Ожидаемые результаты

После применения всех изменений:

### ✅ Пользователь меняет пароль:
1. Фронтенд выводит подробные логи в консоль
2. Edge-функция логирует попытку и результат в serever логи Supabase
3. Запись об изменении пароля появляется в таблице `password_change_log`
4. Пароль успешно обновляется в `auth.users`

### 📊 Теперь возможно:
- Видеть изменения пароля в табице БД
- Отследить, кто меняет пароль (admin или сам пользователь)
- Видеть точное время изменения
- Видеть логи в Supabase (edge function logs)
- Видеть логи в браузер консоли (developer tools)

## Файлы, которые были изменены

1. `supabase/functions/update_user/index.ts` - добавлено логирование и поддержка changed_by
2. `app/users/[id]/edit.jsx` - добавлено поддерблка логирования и информация о том, кто меняет пароль
3. `supabase/migrations/20260211_password_change_log.sql` - новая миграция для таблицы логирования
4. `SETUP_PASSWORD_LOGS.sql` - SQL скрипт для быстрого развертывания (можно выполнить вручную)

## Диагностика проблем

Если после всех изменений все еще не работает, проверьте:

### 1. Логи в браузер консоль
```
[proceedSave] Attempting to update auth credentials
[proceedSave] Calling update_user function at: https://supabase.monitorapp.ru/functions/v1/update_user
[proceedSave] Response status: 200
[proceedSave] Auth update response: { ok: true }
```

### 2. Логи edge-функции в Supabase Dashboard
- Перейти в Edge Functions → update_user → Logs
- Должны видеть логи вида:
```
[UPDATE_USER] Updating auth for user abc-def-ghi: { hasEmail: false, hasPassword: true }
[UPDATE_USER] Auth update successful for user abc-def-ghi
[UPDATE_USER] Logging password change for user abc-def-ghi
[UPDATE_USER] Password change logged successfully for user abc-def-ghi
```

### 3. Проверить, создана ли таблица
```sql
SELECT * FROM information_schema.tables WHERE table_name = 'password_change_log';
-- Должна вернуть 1 результат
```

### 4. Проверить записи в логах
```sql
SELECT * FROM public.password_change_log ORDER BY changed_at DESC LIMIT 5;
-- Должны видеть записи об изменениях паролей
```

## Следующие шаги

1. Применить миграцию `SETUP_PASSWORD_LOGS.sql` в БД
2. Развернуть обновленную edge-функцию
3. Протестировать смену пароля
4. Посмотреть логи в браузер консоль и Supabase Dashboard
5. Убедиться, что запись появляется в `password_change_log`

Если что-то не сработает, проверьте логи - они должны указать на конкретную проблему.
