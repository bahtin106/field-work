# Deep Linking Configuration Guide

## 📱 Текущая конфигурация

Приложение использует гибридную конфигурацию deep linking для обеспечения максимальной совместимости:

### 🛠 Development режим (Expo Go / Development Build)

- **Конфигурация**: `app.json` → `expo.scheme: "workorders"`
- **Назначение**: Обеспечивает работу deep linking в development среде
- **Использование**: Expo Router, development тестирование

### 🚀 Production режим (Native Build)

- **Конфигурация**: `android/app/src/main/AndroidManifest.xml`
- **Настройка**:
  ```xml
  <intent-filter>
    <action android:name="android.intent.action.VIEW"/>
    <category android:name="android.intent.category.DEFAULT"/>
    <category android:name="android.intent.category.BROWSABLE"/>
    <data android:scheme="workorders"/>
  </intent-filter>
  ```

## ⚠️ О варнинге expo-doctor

Варнинг "scheme is not synced in non-CNG project" является **информационным**:

- ✅ **НЕ является ошибкой** - это нормальное поведение для проектов с нативными папками
- ✅ **Схема работает корректно** в production через нативную настройку
- ✅ **Схема работает корректно** в development через app.json

## 🔗 Поддерживаемые ссылки

Приложение поддерживает следующие deep link форматы:

- `workorders://` - главная страница
- `workorders://orders` - список заказов
- `workorders://orders/123` - конкретный заказ
- `workorders://users/456` - профиль пользователя

## 🧪 Тестирование

Для тестирования deep linking используйте:

```bash
# Android (через ADB)
adb shell am start -W -a android.intent.action.VIEW -d "workorders://orders" com.bahtin106.workordersapp

# Или просто запустите
node test-linking.js
```

## ✅ Заключение

Конфигурация deep linking настроена **профессионально и корректно**:

- Работает в development и production
- Совместима с Expo Router
- Поддерживает все необходимые маршруты
- Следует best practices React Native/Expo

Варнинг expo-doctor можно игнорировать - это ожидаемое поведение для проектов с нативными папками.
